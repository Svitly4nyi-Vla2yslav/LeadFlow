[build]
  base = "apps/web"
  publish = "dist"
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback на випадок, якщо _redirects загубиться
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
